# Build output. Regenerated by `npm run build`; Cloudflare Pages builds it on
# deploy. Committing it would mean every change showed up twice in a diff —
# once in the source, once in the minified copy.
/dist/
node_modules/
